{
  "name": "image-recommendation-system",
  "version": "1.0.0",
  "description": "Image recommendation system using ResNet and cosine similarity",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cmake -B build && cmake --build build",
    "build:docker": "mkdir -p build && cd build && cmake .. && make -j$(nproc) && cp feature_extractor .. && cd .. && rm -rf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "image-recommendation",
    "resnet",
    "cosine-similarity",
    "onnx",
    "webassembly"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 